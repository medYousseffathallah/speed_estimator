reconnect:
  enabled: true
  max_retries: 5
  backoff_s: 1.0
  reset_on_success: true

rtsp:
  use_gstreamer: false
  transport: "udp"
  latency_ms: 500
  drop_on_latency: true
  decoder: "avdec_h264"

http:
  enabled: true
